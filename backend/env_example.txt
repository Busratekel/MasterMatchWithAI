# ==========================================
# YASTIK SEÇİM ROBOTU - ENVIRONMENT ÖRNEĞİ
# ==========================================
# Bu dosyayı .env olarak kopyalayın ve gerçek değerlerle doldurun
# ⚠️  HİÇBİR ZAMAN .env dosyasını GitHub'a yüklemeyin!

# ==========================================
# VERİTABANI AYARLARI
# ==========================================
# MSSQL bağlantı stringi (production için)
DATABASE_URL=mssql+pyodbc://username:password@server/database?driver=ODBC+Driver+17+for+SQL+Server

# SQLite (geliştirme için - DATABASE_URL yoksa otomatik kullanılır)
# DATABASE_URL=sqlite:///app.db

# ==========================================
# MAIL AYARLARI (SMTP)
# ==========================================
MAIL_SERVER=smtp.office365.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=your-email@domain.com
MAIL_PASSWORD=your-app-password
MAIL_DEFAULT_SENDER=your-email@domain.com
MAIL_BCC_LIST=admin@domain.com;support@domain.com
MAIL_REPLY_TO=support@domain.com

# ==========================================
# UYGULAMA AYARLARI
# ==========================================
FLASK_ENV=development
FLASK_DEBUG=True
SECRET_KEY=your-super-secret-key-here-change-this

# ==========================================
# CORS AYARLARI
# ==========================================
ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com

# ==========================================
# GÜVENLİK AYARLARI
# ==========================================
# Rate limiting ayarları
RATE_LIMIT_DEFAULT=10 per minute
RATE_LIMIT_RECOMMEND=10 per minute
RATE_LIMIT_KVKK=20 per minute

# ==========================================
# LOGLAMA AYARLARI
# ==========================================
LOG_LEVEL=INFO
LOG_FILE=app.log

# ==========================================
# ÖNEMLİ NOTLAR
# ==========================================
# 1. Bu dosyayı .env olarak kopyalayın
# 2. Tüm değerleri gerçek bilgilerle değiştirin
# 3. .env dosyası .gitignore'da olduğundan emin olun
# 4. Production'da FLASK_ENV=production yapın
# 5. SECRET_KEY'i güçlü bir değerle değiştirin 